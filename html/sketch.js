function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  background(125);
  textSize(50)
 text("p5.js项目展示页", windowWidth/3, windowHeight/3) 
 text("在tkinker页面选择需要显示的程序", windowWidth/3-100, windowHeight/3+80) 
}